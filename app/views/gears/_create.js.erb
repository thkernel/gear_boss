$("ul.errors").html("");
<% if @gear.errors.any? %>
  <% @gear.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".gears").html("<%= escape_javascript(render 'index') %>");
  $("#gear-modal").modal("hide");
 
<% end %>
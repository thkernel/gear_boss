$("ul.errors").html("");
<% if @expense_type.errors.any? %>
  <% @expense_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".expense-types").html("<%= escape_javascript(render 'index') %>");
  $("#expense-type-modal").modal("hide");
 
<% end %>
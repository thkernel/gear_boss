$("ul.errors").html("");
<% if @maintenance_type.errors.any? %>
  <% @maintenance_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".maintenance-types").html("<%= escape_javascript(render 'index') %>");
  $("#maintenance-type-modal").modal("hide");
 
<% end %>